<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:SmartSaleApp.ViewModels"
             x:Class="SmartSaleApp.Pages.HomePage">

    <Grid>
        <VerticalStackLayout Padding="20,20,20,20">
            <HorizontalStackLayout Spacing="20" Padding="0,0,0,20">
                <Entry Text="{Binding BuyerName}" Placeholder="Клиент" WidthRequest="250" />
                <DatePicker Date="{Binding Date, Mode=OneWayToSource}" Format="dd.MMMM.yyyy" />
            </HorizontalStackLayout>

            <CollectionView ItemsSource="{Binding InvoiceDetails}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="8">
                            <Label Text="{Binding Count}" FontSize="18" TextColor="Blue" BackgroundColor="AliceBlue" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>

        

        <Button Text="+ Добавить товар"
                Command="{Binding AddCommand}"
                HorizontalOptions="End" 
                VerticalOptions="End" />

    </Grid>
</ContentPage>